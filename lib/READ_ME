.../lib

This directory contains libraries used internally by the
INGRES compilation scripts.  If you are never going to
recompile INGRES (or are willing to have them rebuilt),
this directory can be cleaned out (although the directory
itself should be left).
